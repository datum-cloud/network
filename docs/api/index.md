# API Reference

API types for the `network.datumapis.com` group, version `v1alpha1`.

## BGP

BGP routing resources for defining routers, peers, policies, advertisements,
and VRF instances.

- [BGP API Reference](bgp.md) — BGPRouter, BGPPeer, BGPPolicy, BGPAdvertisement,
  BGPVRFInstance, BGPCommunitySet, BGPPrefixList, and shared types

## Gateway

Hyperconverged gateway and ingress load-balancing resources.

- [Gateway API Reference](gateway.md) — NetworkGateway, NetworkRule, and
  associated types
